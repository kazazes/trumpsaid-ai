enum Role {
  USER
  ADMIN
}

enum VideoUploadStatus {
  DOWNLOADING
  AWAITING_PROCESSING
  RENDERING
  READY_TO_PUBLISH
  GENERATING_THUMBNAILS
  NEEDS_METADATA
}

enum VideoUploadState {
  PROCESSING
  REJECTED
  FAILED
  PUBLISHED
}

enum AdminRole {
  CREATE_UPLOAD
  LIST_UPLOADS
  LIST_ALL_UPLOADS
  ADVANCE_UPLOADS
  PUBLISH_UPLOAD
}
