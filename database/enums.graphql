enum Role {
  USER
  ADMIN
}

enum VideoUploadStatus {
  AWAITING_PROCESSING
  DOWNLOADING
  RENDERING
  READY_TO_PUBLISH
  GENERATING_THUMBNAILS
  NEEDS_METADATA
}

enum VideoUploadState {
  PENDING
  PROCESSING
  REJECTED
  FAILED
  PUBLISHED
}

enum AdminRole {
  CREATE_UPLOAD
  LIST_UPLOADS
  LIST_ALL_UPLOADS
  ADVANCE_UPLOADS
  PUBLISH_UPLOAD
  DELETE_UPLOADS
}

enum VideoVersion {
  RAW
}
