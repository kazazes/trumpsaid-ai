enum Role {
  USER
  ADMIN
}

enum VideoUploadStatus {
  AWAITING_PROCESSING
  DOWNLOADING
  READY_TO_RENDER
  RENDERING
  NEEDS_THUMBNAILS
  GENERATING_THUMBNAILS
  NEEDS_REVIEW
  NEEDS_METADATA
  READY_TO_PUBLISH
}

enum VideoUploadState {
  PENDING
  PROCESSING
  REJECTED
  FAILED
  PUBLISHED
}

enum AdminRole {
  CREATE_UPLOAD
  LIST_UPLOADS
  LIST_ALL_UPLOADS
  ADVANCE_UPLOADS
  PUBLISH_UPLOAD
  DELETE_UPLOADS
}

enum VideoVersion {
  RAW
  WEBM
  MP4
}
